spring:
  application:
    name: eureka-server-peer1
server:
  port: 10000
eureka:
  instance:
    hostname: eureka-peer1
  client:
    #register-with-eureka: false
    #fetch-registry: false
    service-url:
      defaultZone: http://eureka-peer2:10087/eureka/
  server:
    eviction-interval-timer-in-ms: 20000
    enable-self-preservation: false
