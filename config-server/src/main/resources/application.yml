server:
  port: 7001
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: git@github.com:hw64220/config-server.git
          search-paths: /hello, /eureka-server, /hell_master, /feign_consumer




